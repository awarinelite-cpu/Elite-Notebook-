<?xml version="1.0" encoding="utf-8"?>
<!-- Matches the app's theme_color (#FDD663) so the widget reads as an
     Elite Notebook surface at a glance, same idea as Keep's yellow FAB. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="#FDD663" />
    <corners android:radius="16dp" />
</shape>
